# API Layer

The API layer contains HTTP route declarations, request validation, and response
contracts. It should delegate business behavior to services instead of importing
repositories or external integrations directly.
